# ============================================================
# File: requirements.txt
# Version: 1.1.0
# Created: 2026-06-25
# Modified: 2026-06-25
# Description: Python package dependencies for GMAS (NO anthropic SDK)
# ============================================================

# Web UI
streamlit>=1.35.0

# Database ORM and migrations
sqlalchemy>=2.0.0
alembic>=1.13.0

# Data and analytics
pandas>=2.2.0
plotly>=5.20.0

# Templating
jinja2>=3.1.0

# Settings management
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP client (integrations + grant discovery/scraping)
httpx>=0.27.0

# HTML parsing (grant discovery rules-based extractor)
beautifulsoup4>=4.12.0
lxml>=5.2.0

# PDF export
# NOTE: weasyprint has native GTK/Cairo dependencies on Windows.
# Install via conda: conda install -c conda-forge weasyprint
# Or use: pip install weasyprint (requires GTK runtime on Windows)
weasyprint>=62.0

# YAML parsing (org profile loader)
pyyaml>=6.0.0

# Text similarity (deduplication)
thefuzz>=0.22.0
python-Levenshtein>=0.25.0

# Testing
pytest>=8.0.0
pytest-mock>=3.0.0
pytest-cov>=5.0.0

# Code quality
ruff>=0.4.0
mypy>=1.10.0
