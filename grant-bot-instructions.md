Grant Bot Instructions

1. Run `python app.py` in the project root to query Grants.gov and produce `grant-report.md`.
2. The script searches for opportunities with the keyword "youth development" and writes a markdown report with title, agency, deadline, and application link.
3. If `requests` is missing, install it via `pip install requests`.
